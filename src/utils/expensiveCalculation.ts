/**
 * expensiveCalculation.ts
 *
 * Utility functions for deliberately expensive calculations.
 * Used in Experiment 03 (Heavy JS Calculation) and
 * Experiment 05 (Expensive List Item Rendering).
 *
 * These functions will be implemented when the relevant experiments
 * are built. For now, they export placeholder signatures.
 */

/**
 * Perform a CPU-intensive calculation.
 * Will be implemented in Experiment 03.
 *
 * @param size - Controls the workload size
 * @returns The calculation result
 */
export function heavyCalculation(size: number): number {
  // Placeholder — will be replaced with real expensive work
  return size;
}

/**
 * Perform an expensive per-item transformation.
 * Will be implemented in Experiment 05.
 *
 * @param item - The item to transform
 * @returns Transformed item data
 */
export function expensiveItemTransform(item: unknown): unknown {
  // Placeholder — will be replaced with real expensive work
  return item;
}
